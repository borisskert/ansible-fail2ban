---
dependency:
  name: galaxy

driver:
  name: vagrant
  provider:
    name: virtualbox

platforms:
  - name: ubuntu-bionic
    box: ubuntu/bionic64
    instance_raw_config_args:
      - 'vm.network "private_network", ip: "192.168.33.54"'

provisioner:
  name: ansible

verifier:
  name: ansible
